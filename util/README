statsd.conf is an upstart script. 
If you use upstart, change instances of "/path/to/" to your actual paths,
then put the file in /etc/init/


You can then start statsd like this:

    sudo start statsd



... and stop it like this:
    
    sudo stop statsd

